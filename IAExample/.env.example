# Docker Environment Configuration
# Copy this file to .env and adjust values as needed

# Java Virtual Machine Options
# -Xmx: Maximum heap size
# -Xms: Initial heap size
JAVA_OPTS=-Xmx512m -Xms256m

# Server Port (only used for reference, expose via docker-compose.yml)
SERVER_PORT=8080

# Spring Profile (dev, test, prod)
# SPRING_PROFILES_ACTIVE=prod

# Deepseek API Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Database Configuration (if using external DB in future)
# SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/iaexample
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=password
# SPRING_JPA_HIBERNATE_DDL_AUTO=validate
